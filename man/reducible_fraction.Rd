% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reducible_fraction.R
\name{reducible_fraction}
\alias{reducible_fraction}
\title{Reducible uncertainty fraction}
\usage{
reducible_fraction(ti_bound, pi_bound, median)
}
\arguments{
\item{ti_bound}{Numeric vector of tolerance-interval bounds.}

\item{pi_bound}{Numeric vector of prediction-interval bounds.}

\item{median}{Numeric vector of point estimates (medians).}
}
\value{
Numeric vector of reducible fractions, with \code{NA} where
\code{ti_bound == median}.
}
\description{
Computes the fraction of the tolerance-interval gap that is not covered by
the prediction interval, i.e. \code{(TI - PI) / (TI - Median)}.
}
\details{
Guards against a zero-width interval (a tolerance-interval bound equal to the
median), which would otherwise divide by zero and yield a silent \code{Inf}/\code{NaN};
such degenerate rows return \code{NA} instead.

Callers are responsible for any further scaling (e.g. to a percentage) and
rounding.
}
\examples{
reducible_fraction(10, 8, 4)
# NA when the tolerance-interval bound equals the median
reducible_fraction(5, 5, 5)
# Vectorised over its arguments
reducible_fraction(c(10, 5), c(8, 5), c(4, 5))
}
