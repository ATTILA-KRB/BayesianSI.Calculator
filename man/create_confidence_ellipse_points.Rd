% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/quantile_scatterplot.R
\name{create_confidence_ellipse_points}
\alias{create_confidence_ellipse_points}
\title{Compute points tracing a confidence ellipse for a 2D covariance matrix}
\usage{
create_confidence_ellipse_points(
  cov_matrix,
  centre,
  level = 0.95,
  npoints = 100
)
}
\arguments{
\item{cov_matrix}{A 2x2 covariance matrix.}

\item{centre}{Numeric vector of length 2 giving the ellipse centre.}

\item{level}{Confidence level strictly between 0 and 1. Default is 0.95.}

\item{npoints}{Number of points used to trace the ellipse. Must be >= 4.
Default is 100.}
}
\value{
A matrix with columns \code{"x"} and \code{"y"} giving the ellipse
coordinates.
}
\description{
Helper used by \code{\link[=quantile_scatterplot]{quantile_scatterplot()}} to draw credible ellipses around the
lower/upper quantile clouds. Negative eigenvalues arising from degenerate or
numerically non-positive-definite covariance matrices are clamped to zero so
the ellipse degrades gracefully rather than erroring.
}
\examples{
create_confidence_ellipse_points(matrix(c(1, 0, 0, 1), 2), c(0, 0))
create_confidence_ellipse_points(
  cov_matrix = matrix(c(2, 0.5, 0.5, 1), 2),
  centre = c(1, -1),
  level = 0.9,
  npoints = 200
)

}
